# 插件验证记录

版本：0.1.2；日期：2026-09-15；定位：内部试用。

本文件仅记录插件打包和安装验收。业务资料来自此前 20 应用盘点，59 个业务闭环仍未端到端验收。

## 本轮结果

- 0.1.2 将总入口拆成两条按需加载的客户旅程：未注册客户的公开场景诊断/模块选型，以及已注册租户的能力评估/培训/授权操作。
- 未注册模式复用官网当前公开的六条业务旅程和产品入口，不要求登录、不把内部手册能力当作客户已开通能力；租户模式只按实时可见权限生成能力清单。
- 0.1.1 将市场展示、技能发现描述和默认提示改为英文；运行时按用户最新请求自动使用英文、泰文或中文，无法判断时默认英文。
- 操作手册继续作为单一事实源；英文/泰文回答即时翻译，并保留当前页面的真实标签、URL、记录号和错误原文。
- Codex 插件声明：校验通过。
- Claude Code 插件声明：严格校验通过。
- Claude marketplace：首次严格校验发现缺少目录描述警告，已补充；严格重测通过。
- 21 技能：逐个运行 skill-creator 的 quick_validate.py，全部通过；四项操作结构完整。
- 包内容：354 个唯一页面操作卡、59 个唯一核心任务、455 个本地文件/显式锚点引用通过；发布白名单 53 文件。插件内部所有相对引用都留在插件根目录内。
- 包检查脚本的 6 个负向内存变体：断链、包外引用、缺锚点、两端版本不一致、缺任务、疑似凭证模式均被拒绝；没有把这些变体写入发布内容。
- Codex CLI 0.144.4：0.1.0 基线版本的本地安装曾验证 installed=true、enabled=true；0.1.2 需在新任务中复测双客户旅程和语言触发。
- Claude Code 2.1.185：0.1.0 基线版本的 user 范围安装曾验证 enabled=true；0.1.2 需在新会话中复测双客户旅程和语言触发。
- 0.1.0 基线版的两端安装缓存曾验证为各 46 个插件文件、21 份 SKILL.md，并与当时源包逐文件 SHA-256 一致；0.1.1 未复用该结论。
- 0.1.2 ZIP 预检：53 文件、CRC 通过；临时独立目录解压后包检查、Codex 声明校验、Claude 插件及市场严格校验均通过。未调用 ERP 后端；临时验证文件已自动清理。

复核命令（在 marketplace 目录执行）：

```sh
python3 scripts/check-package.py
claude plugin validate . --strict
claude plugin validate ./plugins/petaos-onboarding --strict
codex plugin list --marketplace petaos --json
claude plugin list --json
```

插件安装状态是本机证据，不代表客户机器已安装。启动新任务/会话后才进行技能触发和浏览器交互验收；本轮没有启动新模型会话执行自动业务。

## 尚未验证

- 未注册客户场景诊断的真实对话质量、官网内容变化后的推荐一致性，以及预约演示的实际提交。
- 新任务中技能的真实触发和选择体验，以及 Claude/Codex 各自浏览器工具的交互行为。
- 英文、泰文和中英文混合请求的真实模型回复质量；静态语言规则不等于三语行为已验收。
- 已登录租户中的流程保存、发布、申请、跨角色审批及其他全业务链；本轮没有业务写入。
- 无浏览器、未登录、权限不足、未开通、业务超时的真实交互演练。文案约束/静态检查不等于模型已按预期执行。
- 其他操作系统、其他宿主版本、客户账号套餐、物理硬件及第三方支付/外发通道。
- 之前 ERP 代码修复的线上部署和受保护页面回归；本插件不包含这些修复代码。
